# frozen_string_literal: true

RSpec.describe Govuk::Forms::Markdown::Renderer do
  it "has a version number" do
    expect(Govuk::Forms::Markdown::Renderer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
